<%= form_for(@song) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :artist %>
  <%= f.collection_select :tag_id, Tag.order(:name),:id,:name, include_blank: true %>
<%= f.submit  %>
<% end %>
